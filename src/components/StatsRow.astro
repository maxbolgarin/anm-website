---
interface StatItem {
  value: string | number;
  label: string;
  unit?: string;
}

interface Props {
  stats: StatItem[];
  columns?: number;
}

const { stats, columns } = Astro.props;
---

<div class="stats-row" data-columns={columns}>
  {stats.map((stat) => (
    <div class="stats-row__item">
      <span class="stats-row__value">
        {stat.value}
        {stat.unit && <span class="stats-row__unit">{stat.unit}</span>}
      </span>
      <span class="stats-row__label">{stat.label}</span>
    </div>
  ))}
</div>

<style>
  /* Default: all items in one flexible row */
  .stats-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(0, auto));
    gap: var(--space-lg) var(--space-2xl);
    justify-content: start;
  }

  /* Fixed 2-column grid (for building pages with 4 stats) */
  .stats-row[data-columns="2"] {
    grid-template-columns: auto auto;
  }

  .stats-row__item {
    display: flex;
    flex-direction: column;
  }

  .stats-row__value {
    font-family: var(--font-heading);
    font-size: var(--text-heading);
    font-weight: var(--weight-bold);
    line-height: 1.0;
    letter-spacing: -0.02em;
  }

  .stats-row__unit {
    font-size: 0.875em;    /* Relative to value size, not absolute */
    font-weight: var(--weight-regular);
    color: var(--color-text-secondary);
    letter-spacing: 0;
  }

  .stats-row__label {
    font-size: var(--text-caption);
    font-weight: var(--weight-medium);
    color: var(--color-text-secondary);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-top: var(--space-xs);
    white-space: nowrap;
  }

  @media (max-width: 767px) {
    .stats-row {
      grid-template-columns: repeat(2, auto);
      gap: var(--space-lg) var(--space-xl);
    }

    .stats-row__value {
      font-size: clamp(1.25rem, 3vw, 1.5rem);
    }
  }
</style>
