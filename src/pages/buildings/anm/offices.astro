---
import BuildingLayout from '@layouts/BuildingLayout.astro';
import OfficeFilter from '@components/islands/OfficeFilter';
import '@components/islands/OfficeFilter.css';
import ContactCTA from '@components/ContactCTA.astro';
import { getBuildingBySlug } from '@data/buildings';

const building = getBuildingBySlug('anm')!;

const title = `Аренда офисов в БЦ ${building.name} — свободные помещения | АНМ`;
const description = `Аренда офисов в бизнес-центре ${building.name}. Класс B, современные офисные помещения от 55 до 95 м². Свяжитесь с нами для обсуждения вариантов.`;

const typicalOffices = [
  {
    label: 'Пример типового офиса в бизнес-центре АНМ',
    planImage: '/plans/anm plan example.png',
    area: 'от 55 до 95 м²',
    buildingClass: 'B',
    type: 'Кабинет / Open Space',
    pricePerSqm: 'от 1 000 ₽/м²',
    href: '/buildings/anm/',
  },
];
---

<BuildingLayout
  title={title}
  description={description}
  buildingName={building.name}
  buildingSlug={building.slug}
>
  <section class="offices-section">
    <div class="container">
      <h1 class="offices-section__title">Аренда офисов в БЦ АНМ</h1>

      <OfficeFilter
        client:visible
        buildingSlug="anm"
        contactPhone="+7 (812) 336-55-64"
        contactEmail="arenda@ukanm.ru"
        typicalOffices={typicalOffices}
      />
    </div>
  </section>

  <ContactCTA />
</BuildingLayout>

<style>
  .offices-section {
    padding-top: var(--space-xl);
    padding-bottom: var(--space-3xl);
  }

  .offices-section__title {
    font-size: var(--text-h1);
    margin-bottom: var(--space-2xl);
  }
</style>
